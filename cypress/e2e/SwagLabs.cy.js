describe('template spec', () => {

  beforeEach(() => {
    cy.visit(Cypress.config('baseUrl'),{ timeout: 60000 })
  });



  it('Testando Adicionar produtos no carrinho', () => {
  
    //cy.get(':nth-child(1) > .pricebar > .btn_primary').click().should('be.visible')
    //cy.get('#shopping_cart_container').click()

  
  })
})